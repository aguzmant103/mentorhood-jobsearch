[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn backend.health:app --host 0.0.0.0 --port ${PORT}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3 